{
  "name": "smart-doc-qa-workspace",
  "version": "1.0.0",
  "description": "Smart Doc Q&A Workspace - Run frontend and backend concurrently",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\"",
    "start": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true
}
